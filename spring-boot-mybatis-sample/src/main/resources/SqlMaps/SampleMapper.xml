<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sample.db.mapper.SampleMapper">

	<resultMap id="BaseResultMap" type="Sample">
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="firstName" jdbcType="VARCHAR" property="firstName" />
		<result column="lastName" jdbcType="VARCHAR" property="lastName" />
		<result column="email" jdbcType="VARCHAR" property="email" />
	</resultMap>

	<select id="getAll"  resultMap="BaseResultMap">
		SELECT id, firstName, lastName, email FROM sample
	</select>

	<insert id="insert" parameterType="Sample">
		INSERT INTO sample (firstName, lastName, email)
		VALUES (#{firstName}, #{lastName}, #{email})
	</insert>

	<update id="update" parameterType="Sample">
		UPDATE sample SET
		firstName = #{firstName},
		lastName = #{lastName},
		email = #{email}
		WHERE ID = #{id}
	</update>
</mapper>